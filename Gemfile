source 'https://rubygems.org'

group :development do
  # Write unit tests.
  gem 'rspec' 

  # Debugger for troubleshooting.
  gem 'byebug'
end

group :production do
  # Enable internationalization and localization for literals.
  gem 'i18n'
end